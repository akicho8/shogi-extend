// import { ParamInfo } from "./models/param_info.js"

export const app_vars = {
  // methods: {
  //   created_then_vars_setup() {
  //     // ParamInfo.values.forEach(e => {
  //     //   const default_value = e.defaults[this.$config.STAGE] ?? e.defaults["production"]
  //     //   this.$set(this.DEFAULT_VARS, e.key, default_value)
  //     //   let v = this.$route.query[e.key] ?? this.DEFAULT_VARS[e.key]
  //     //   if (e.type === "string") {
  //     //   } else if (e.type === "float") {
  //     //     v = parseFloat(v)
  //     //   } else if (e.type === "integer") {
  //     //     v = parseInt(v)
  //     //   } else {
  //     //     throw new Error(`${e.type}`)
  //     //   }
  //     //   this.$data[e.key] = v
  //     // })
  //   },
  // },
  // computed: {
  //   // ParamInfo() { return ParamInfo },
  //   // pc_attributes() { return ParamInfo.values.reduce((a, e) => ({...a, [e.key]: this.$data[e.key]}), {}) },
  // },
}
