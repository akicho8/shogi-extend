import { OrderTabInfo } from "@/components/ShareBoard/mod_order/order_tab_info.js"

describe("OrderTabInfo", () => {
  test("works", () => {
    expect(OrderTabInfo.values.length > 0).toEqual(true)
  })
})
