- @page_title ||= controller.current_page_title

- ogp_params_set(controller.twitter_card_options)

- content_for :head do
  javascript:
    document.addEventListener('DOMContentLoaded', () => { new Vue().$mount("#app") })

#app
  share_board(:info="#{controller.info_params.to_json}")
